import variables

import tkinter as tk

import GUImanager

variables.root.geometry(str(variables.size_x) + 'x' + str(variables.size_y))
variables.root.minsize(str(variables.size_x), str(variables.size_y))
variables.root.maxsize(str(variables.size_x),str(variables.size_y))
variables.root.title("Job Portal")
#root.iconbitmap('assist.ico')
#FramePlacer(frame1)

variables.bgActivity.start()
variables.root.mainloop()